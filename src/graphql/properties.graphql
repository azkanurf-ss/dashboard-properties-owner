query GetAllProperties {
  GetProperties(pageSize: 5) {
    pageSize
    pageNumber
    pageCount
    totalCount
    _embedded {
      id
      currency
      selling {
        price
      }
      address {
        countryId
        line1
      }
      bedrooms
      bathrooms
      description
      negotiatorId
    }
  }
}
